build:
  image: python:3.6-alpine
  script:
    - apk add --no-cache gcc musl-dev libffi-dev python-dev
    - pip3 install wheel setuptools twine
    - python3 setup.py sdist bdist_wheel
    - twine upload dist/*
  only:
    - master
